var firstName = 'John';
console.log(firstName);
var lastName = 'Smith';
var age = 28;

//Primitive(non-object) js data types : Number, String, Boolean, Undefined(data type that does not have a value yet), Null(non-existent)

var fullAge = true;
var job; //Undefined
console.log(job);
job = 'Teacher';
console.log(job);